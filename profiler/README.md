### How to use
```
<binary_path> --pid <process_pid> --interval-ms <interval-time> --out <output_log>
```

Using cargo to run the program
```
cargo run --release -- --pid 1 --interval-ms 1000 --out output.json
```